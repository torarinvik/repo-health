[project]
name = "membership-fixture"
version = "1.0.0"
dependencies = [
  "Attrs==1.0",
  "requests>=2,<3",
  "missing==1.0",
  "attrs==9.9",
  "ambiguous_consumer==3.0.0",
  "private @ https://user:secret@example.invalid/pkg.whl",
]

[project.optional-dependencies]
"test-suite" = ["pytest==7.0"]
