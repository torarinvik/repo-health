{
  "schema": "rh-connector-instance/1",
  "connector_id": "forgejo",
  "base_url": "https://127.0.0.1:3000",
  "approved": true,
  "capabilities": ["history"],
  "note": "Negative fixture: an approved instance whose base URL is loopback space. Transport policy is a separate gate and must reject it regardless of approval (S002); approval never overrides transport."
}
