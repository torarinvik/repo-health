{
  "schema": "rh-jsonschema/1",
  "name": "registry-meta",
  "targets": ["fixtures/packages/registry-meta.json"],
  "type": "object",
  "required": ["versions"],
  "types": {"versions": "list"},
  "items": {
    "versions": {"required": ["num"], "types": {"num": "str"}}
  }
}
