{
  "schema": "rh-jsonschema/1",
  "name": "postgres-staged-gitlab-issues-input",
  "targets": ["fixtures/postgres/staged-gitlab-issues-input.json"],
  "required": ["schema", "source_id", "collection_run_id", "provider", "captured_at", "authorization", "collector_label", "canonical_capability", "pages"],
  "types": {
    "schema": "str", "source_id": "str", "collection_run_id": "str", "provider": "str", "captured_at": "int",
    "authorization": "dict", "collector_label": "str", "canonical_capability": "str", "pages": "list"
  },
  "enum": {
    "schema": ["rh-postgres-staged-gitlab-issues-input/1"],
    "provider": ["gitlab"],
    "canonical_capability": ["issues"]
  },
  "nested": {
    "authorization": {"required": ["state"], "types": {"state": "str"}, "enum": {"state": ["authorized", "unauthorized", "not_requested", "unknown"]}}
  },
  "items": {
    "pages": {
      "required": ["page_number", "committed", "completeness", "records"],
      "types": {"page_number": "int", "committed": "bool", "completeness": "str", "records": "list"},
      "enum": {"completeness": ["complete", "empty"]},
      "items": {
        "records": {
          "required": ["record_ordinal", "collector_label", "raw_payload"],
          "types": {"record_ordinal": "int", "collector_label": "str", "raw_payload": "str"}
        }
      }
    }
  }
}
