{
  "schema": "rh-jsonschema/1",
  "name": "distribution",
  "targets": ["fixtures/distribution/*-result.json"],
  "type": "object",
  "required": ["schema", "format", "source_stanzas", "binary_packages", "unknown_fields", "unresolved_relations", "note"],
  "types": {
    "schema": "str",
    "format": "str",
    "source_stanzas": "list",
    "binary_packages": "list",
    "unknown_fields": "int",
    "unresolved_relations": "int",
    "note": "str"
  },
  "enum": {
    "schema": ["rh-distribution-result/1"],
    "format": ["debian-control"]
  },
  "items": {
    "source_stanzas": {
      "required": ["name", "version", "build_relations", "source_assertions"],
      "types": {"name": "str", "version": "str_or_null", "build_relations": "list", "source_assertions": "list"}
    },
    "binary_packages": {
      "required": ["name", "version", "architecture", "source", "provides", "relations"],
      "types": {"name": "str", "version": "str_or_null", "architecture": "str_or_null", "source": "str_or_null", "provides": "str_or_null", "relations": "list"}
    }
  }
}
