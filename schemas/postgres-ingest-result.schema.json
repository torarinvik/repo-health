{
  "schema": "rh-jsonschema/1",
  "name": "postgres-ingest-result",
  "targets": ["fixtures/postgres/ingest-output.json", "fixtures/postgres/ingest-partial-output.json"],
  "required": ["schema", "operation", "status", "source_id", "run_id", "job_id", "fencing_token", "pages", "lease_expires_at"],
  "types": {
    "schema": "str", "operation": "str", "status": "str", "source_id": "str", "run_id": "str", "job_id": "str",
    "fencing_token": "int", "pages": "list", "lease_expires_at": "str"
  },
  "enum": {"schema": ["rh-postgres-ingest-result/1"], "operation": ["ingest"], "status": ["succeeded", "partial", "not_claimed"]},
  "items": {
    "pages": {
      "required": ["page_number", "status"],
      "types": {"page_number": "int", "status": "str"},
      "enum": {"status": ["committed", "duplicate"]}
    }
  }
}
