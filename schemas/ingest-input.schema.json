{
  "schema": "rh-jsonschema/1",
  "name": "ingest-input",
  "targets": ["fixtures/ingest/conformance-input.json"],
  "required": ["schema", "source", "capability", "owner", "lease_now", "lease_ttl", "collection_start", "pages"],
  "types": {
    "schema": "str",
    "source": "str",
    "capability": "str",
    "owner": "str",
    "lease_now": "int",
    "lease_ttl": "int",
    "collection_start": "int",
    "collection_complete": "bool",
    "pages": "list"
  },
  "enum": {"schema": ["rh-ingest-input/1"]},
  "items": {
    "pages": {
      "required": ["page", "status", "commit", "events"],
      "types": {"page": "int", "status": "str", "commit": "bool", "events": "list", "failure_kind": "str", "replaces_page": "int", "token": "int"},
      "enum": {
        "status": ["complete", "empty", "failed", "partial"],
        "failure_kind": ["rate_limit", "authorization", "unsupported", "transient"]
      },
      "items": {
        "events": {
          "required": ["id", "line"],
          "types": {"id": "str", "line": "str"}
        }
      }
    }
  }
}
