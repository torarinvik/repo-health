{
  "schema": "rh-jsonschema/1",
  "name": "snapshot-reconcile-result",
  "targets": ["fixtures/connectors/snapshot-reconcile-result.json"],
  "required": ["schema", "scope", "captured_at", "acquisition", "successful_acquisition", "refresh_last_success", "clear_unseen_in_scope", "counts", "records", "note"],
  "types": {
    "schema": "str",
    "scope": "dict",
    "captured_at": "int",
    "acquisition": "str",
    "successful_acquisition": "bool",
    "refresh_last_success": "bool",
    "clear_unseen_in_scope": "bool",
    "counts": "dict",
    "records": "list",
    "note": "str"
  },
  "enum": {
    "schema": ["rh-snapshot-reconcile-result/1"],
    "acquisition": ["complete", "empty", "partial", "failed", "unsupported"]
  },
  "nested": {
    "scope": {
      "required": ["source", "capability", "scope"],
      "types": {"source": "str", "capability": "str", "scope": "str"}
    },
    "counts": {
      "required": ["prior", "observed", "absent", "retained_unconfirmed", "current"],
      "types": {"prior": "int", "observed": "int", "absent": "int", "retained_unconfirmed": "int", "current": "int"}
    }
  },
  "items": {
    "records": {
      "required": ["native_id", "state"],
      "types": {"native_id": "str", "state": "str"},
      "enum": {"state": ["present", "absent", "retained_unconfirmed"]}
    }
  }
}
