{
  "schema": "rh-jsonschema/1",
  "name": "snapshot-reconcile-input",
  "targets": ["fixtures/connectors/snapshot-reconcile-input.json"],
  "required": ["schema", "source", "capability", "scope", "captured_at", "acquisition", "prior", "observed"],
  "types": {
    "schema": "str",
    "source": "str",
    "capability": "str",
    "scope": "str",
    "captured_at": "int",
    "acquisition": "str",
    "prior": "list",
    "observed": "list"
  },
  "enum": {
    "schema": ["rh-snapshot-reconcile-input/1"],
    "acquisition": ["complete", "empty", "partial", "failed", "unsupported"]
  }
}
