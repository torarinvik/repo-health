{
  "schema": "rh-jsonschema/1",
  "name": "postgres-staged-normalize-result",
  "targets": ["fixtures/postgres/staged-github-issues-output.json"],
  "required": ["schema", "source_id", "collection_run_id", "input_sha256", "provider", "collector_label", "canonical_capability", "record_count", "pages", "staged_records", "normalizer", "normalized"],
  "types": {
    "schema": "str", "source_id": "str", "collection_run_id": "str", "input_sha256": "str", "provider": "str",
    "collector_label": "str", "canonical_capability": "str", "record_count": "int", "pages": "list", "staged_records": "list", "normalizer": "str", "normalized": "dict", "note": "str"
  },
  "enum": {
    "schema": ["rh-postgres-staged-normalize-result/1"],
    "provider": ["github"],
    "canonical_capability": ["issues"],
    "normalizer": ["rh-forge-events/1"]
  },
  "items": {
    "pages": {"required": ["page_number", "completeness", "record_count"], "types": {"page_number": "int", "completeness": "str", "record_count": "int"}},
    "staged_records": {"required": ["page_number", "record_ordinal"], "types": {"page_number": "int", "record_ordinal": "int"}}
  },
  "nested": {
    "normalized": {
      "required": ["schema", "provider", "captured_at", "authorization", "scope", "capabilities", "events"],
      "types": {"schema": "str", "provider": "str", "captured_at": "int", "authorization": "dict", "scope": "dict", "capabilities": "dict", "events": "list"},
      "enum": {"schema": ["rh-forge-events-result/1"], "provider": ["github"]},
      "nested": {
        "capabilities": {
          "nested": {
            "issues": {"required": ["status", "attempted", "normalized", "duplicate_replacements", "count", "rejected"], "types": {"status": "str", "attempted": "int", "normalized": "int", "duplicate_replacements": "int", "count": "int", "rejected": "int"}}
          }
        }
      }
    }
  }
}
