{
  "schema": "rh-jsonschema/1",
  "name": "github-collaborator-probe-result",
  "targets": ["fixtures/connectors/github-collaborator-probe-result.json"],
  "required": ["schema", "provider", "captured_at", "scope", "authorization", "capabilities", "evidence", "note"],
  "types": {
    "schema": "str", "provider": "str", "captured_at": "int", "scope": "dict",
    "authorization": "dict", "capabilities": "dict", "evidence": "dict", "note": "str"
  },
  "enum": {
    "schema": ["rh-github-collaborator-probe-result/1"],
    "provider": ["github"]
  },
  "nested": {
    "scope": {"required": ["repository"], "types": {"repository": "str"}},
    "authorization": {
      "required": ["credential"], "types": {"credential": "str"},
      "enum": {"credential": ["configured"]}
    },
    "capabilities": {
      "required": ["collaborators"], "types": {"collaborators": "dict"},
      "nested": {
        "collaborators": {
          "required": ["declaration", "status", "http_status", "coverage_state"],
          "types": {"declaration": "str", "status": "str", "http_status": "int_or_null", "coverage_state": "str"},
          "enum": {
            "declaration": ["read-one-item-authenticated"],
            "status": ["observed", "malformed", "unauthorized", "forbidden_or_rate_limited", "not_found_or_private", "rate_limited", "unavailable"],
            "coverage_state": ["observed", "partial", "unauthorized", "unavailable"]
          }
        }
      }
    },
    "evidence": {
      "required": ["url", "body", "status", "error"],
      "types": {"url": "str", "body": "str", "status": "str", "error": "str"}
    }
  }
}
