{
  "schema": "rh-jsonschema/1",
  "name": "forge-events-result",
  "targets": ["fixtures/connectors/forge-events-result.json"],
  "required": ["schema", "provider", "captured_at", "authorization", "capabilities", "events", "note"],
  "types": {
    "schema": "str", "provider": "str", "captured_at": "int", "authorization": "dict",
    "capabilities": "dict", "events": "list", "note": "str"
  },
  "enum": {
    "schema": ["rh-forge-events-result/1"],
    "provider": ["github", "gitlab", "gitea", "forgejo"]
  },
  "nested": {
    "authorization": {
      "required": ["state"], "types": {"state": "str"},
      "enum": {"state": ["authorized", "unauthorized", "not_requested", "unknown"]}
    },
    "capabilities": {
      "required": ["issues", "proposals", "reviews", "releases"], "types": {
        "issues": "dict", "proposals": "dict", "reviews": "dict", "releases": "dict"
      },
      "nested": {
        "issues": {"required": ["status", "count", "rejected"], "types": {"status": "str", "count": "int_or_null", "rejected": "int"}, "enum": {"status": ["observed", "unsupported"]}},
        "proposals": {"required": ["status", "count", "rejected"], "types": {"status": "str", "count": "int_or_null", "rejected": "int"}, "enum": {"status": ["observed", "unsupported"]}},
        "reviews": {"required": ["status", "count", "rejected"], "types": {"status": "str", "count": "int_or_null", "rejected": "int"}, "enum": {"status": ["observed", "unsupported"]}},
        "releases": {"required": ["status", "count", "rejected"], "types": {"status": "str", "count": "int_or_null", "rejected": "int"}, "enum": {"status": ["observed", "unsupported"]}}
      }
    }
  },
  "items": {
    "events": {
      "required": ["kind", "native_id", "status", "created_at", "updated_at", "closed_at"],
      "types": {"kind": "str", "native_id": "str", "status": "str", "created_at": "int", "updated_at": "int_or_null", "closed_at": "int_or_null", "tag": "str", "url": "str"},
      "enum": {"kind": ["issues", "proposals", "reviews", "releases"]}
    }
  }
}
