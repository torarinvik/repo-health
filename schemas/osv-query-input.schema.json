{
  "schema": "rh-jsonschema/1",
  "name": "osv-query-input",
  "targets": ["fixtures/packages/osv-query-cargo.json"],
  "type": "object",
  "required": ["schema", "package", "version"],
  "types": {"schema": "str", "package": "dict", "version": "str"},
  "enum": {"schema": ["rh-osv-query-input/1"]},
  "nested": {
    "package": {
      "required": ["ecosystem", "name"],
      "types": {"ecosystem": "str", "name": "str"},
      "enum": {"ecosystem": ["cargo", "npm", "pypi", "go", "rubygems", "composer", "nuget", "maven"]}
    }
  }
}
