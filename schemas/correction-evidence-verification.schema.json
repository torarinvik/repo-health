{
  "schema": "rh-jsonschema/1",
  "name": "correction-evidence-verification",
  "targets": ["fixtures/m06/correction-evidence-verification.json"],
  "type": "object",
  "required": ["schema", "status", "evaluated_at", "correction_count", "input_sha256", "policy_sha256"],
  "types": {"schema": "str", "status": "str", "evaluated_at": "int", "correction_count": "int", "input_sha256": "str", "policy_sha256": "str"},
  "enum": {"schema": ["rh-correction-evidence-verification/1"], "status": ["policy_satisfied"]}
}
