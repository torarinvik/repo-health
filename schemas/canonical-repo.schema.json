{
  "schema": "rh-jsonschema/1",
  "name": "canonical-repo",
  "targets": ["fixtures/connectors/*.canonical.json"],
  "type": "object",
  "required": ["schema", "source", "native_id", "created_at"],
  "types": {"schema": "str", "source": "str", "native_id": "str", "created_at": "int"},
  "enum": {"schema": ["rh-canonical-repo/1"]}
}
