{
  "schema": "rh-jsonschema/1",
  "name": "postgres-result",
  "targets": ["fixtures/postgres/*-result.json"],
  "required": ["schema", "operation", "status"],
  "types": {
    "schema": "str",
    "operation": "str",
    "status": "str",
    "job_id": "str",
    "fencing_token": "int",
    "lease_expires_at": "str"
  },
  "enum": {
    "schema": ["rh-postgres-result/1"],
    "operation": ["page_commit", "page_commit_events", "claim_job", "heartbeat_job", "finish_job"],
    "status": ["committed", "duplicate", "claimed", "empty", "applied", "fenced"]
  }
}
