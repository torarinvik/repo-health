{
  "schema": "rh-jsonschema/1",
  "name": "postgres-staged-github-issues-input",
  "targets": ["fixtures/postgres/staged-github-issues-input.json"],
  "required": ["schema", "source_id", "collection_run_id", "provider", "captured_at", "authorization", "scope", "collector_label", "canonical_capability", "pages"],
  "types": {
    "schema": "str", "source_id": "str", "collection_run_id": "str", "provider": "str", "captured_at": "int",
    "authorization": "dict", "scope": "dict", "collector_label": "str", "canonical_capability": "str", "pages": "list"
  },
  "enum": {
    "schema": ["rh-postgres-staged-github-issues-input/1"],
    "provider": ["github"],
    "canonical_capability": ["issues"]
  },
  "nested": {
    "authorization": {"required": ["state"], "types": {"state": "str"}, "enum": {"state": ["authorized", "unauthorized", "not_requested", "unknown"]}},
    "scope": {"required": ["repository"], "types": {"repository": "str"}}
  },
  "items": {
    "pages": {
      "required": ["page_number", "committed", "completeness", "records"],
      "types": {"page_number": "int", "committed": "bool", "completeness": "str", "evidence_id": "str_or_null", "records": "list"},
      "enum": {"completeness": ["complete", "empty"]},
      "items": {
        "records": {
          "required": ["record_ordinal", "collector_label", "raw_payload"],
          "types": {"record_ordinal": "int", "collector_label": "str", "raw_payload": "str"}
        }
      }
    }
  }
}
