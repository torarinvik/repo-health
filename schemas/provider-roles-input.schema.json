{
  "schema": "rh-jsonschema/1",
  "name": "provider-roles-input",
  "targets": ["fixtures/roles/*-provider-input.json"],
  "type": "object",
  "required": ["schema", "provider", "authorization", "captured_at", "members"],
  "types": {"schema": "str", "provider": "str", "authorization": "dict", "captured_at": "int", "members": "list"},
  "enum": {"schema": ["rh-provider-roles-input/1"], "provider": ["github", "gitlab"]},
  "nested": {
    "authorization": {"required": ["state"], "types": {"state": "str"}},
    "pagination": {
      "required": ["next", "complete"],
      "types": {"next": "str_or_null", "complete": "bool"}
    }
  },
  "items": {"members": {"required": ["id"], "types": {"id": "int"}}}
}
