{
  "schema": "rh-jsonschema/1",
  "name": "ecosystem-lookup-result",
  "targets": ["fixtures/connectors/ecosystem-lookup-result.json"],
  "required": ["schema", "provider", "query", "origin", "results", "rejected", "note"],
  "types": {
    "schema": "str",
    "provider": "str",
    "query": "dict",
    "origin": "dict",
    "results": "list",
    "rejected": "int",
    "note": "str"
  },
  "enum": {
    "schema": ["rh-ecosystem-lookup-result/1"],
    "provider": ["ecosyste.ms"]
  },
  "nested": {
    "query": {
      "required": ["ecosystem", "name"],
      "types": {"ecosystem": "str", "name": "str"}
    },
    "origin": {
      "required": ["transport", "source_url", "status", "data_license", "license_url", "attribution_required", "attribution_url", "modifications_made", "modification_note"],
      "types": {"transport": "str", "source_url": "str_or_null", "status": "str_or_null", "data_license": "str", "license_url": "str", "attribution_required": "bool", "attribution_url": "str", "modifications_made": "bool", "modification_note": "str"},
      "enum": {"transport": ["captured", "live"], "data_license": ["CC-BY-SA-4.0"]}
    }
  },
  "items": {
    "results": {
      "required": ["provider_id", "name", "ecosystem", "version", "published_at", "repository", "homepage", "manifest", "versions_count", "mapping", "coverage"],
      "types": {
        "provider_id": "int",
        "name": "str",
        "ecosystem": "str",
        "version": "str_or_null",
        "published_at": "str_or_null",
        "repository": "str_or_null",
        "homepage": "str_or_null",
        "manifest": "str_or_null",
        "versions_count": "int_or_null",
        "mapping": "dict",
        "coverage": "dict"
      },
      "nested": {
        "mapping": {
          "required": ["status", "basis"],
          "types": {"status": "str", "basis": "str"},
          "enum": {"status": ["candidate"]}
        },
        "coverage": {
          "required": ["status", "fields"],
          "types": {"status": "str", "fields": "list"},
          "enum": {"status": ["observed"]}
        }
      }
    }
  }
}
