{
  "schema": "rh-jsonschema/1",
  "name": "postgres-legacy-ingest-input",
  "targets": ["fixtures/ingest/postgres-bridge-replay.json"],
  "required": ["schema", "postgres_input_json", "legacy_input_json", "legacy_result_json", "scope_hash", "committed_attempts"],
  "types": {
    "schema": "str", "postgres_input_json": "str", "legacy_input_json": "str",
    "legacy_result_json": "str", "scope_hash": "str", "committed_attempts": "list"
  },
  "enum": {"schema": ["rh-postgres-legacy-ingest-input/1"]},
  "items": {
    "committed_attempts": {
      "required": ["legacy_attempt", "cursor_before_json", "cursor_after_json", "committed_at"],
      "types": {
        "legacy_attempt": "int", "cursor_before_json": "str",
        "cursor_after_json": "str", "committed_at": "str"
      }
    }
  }
}
