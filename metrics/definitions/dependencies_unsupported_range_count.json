{
  "key": "dependencies.unsupported_range_count",
  "version": "1.0.0",
  "implementation_status": "implemented",
  "owner": "metrics-maintainers",
  "subject_kind": "package",
  "output": {
    "unit": "count",
    "numerator": null,
    "denominator": null
  },
  "denominator_rule": "none",
  "source_requirements": [
    "generic-git"
  ],
  "inputs": [
    "resolved_graph",
    "unresolved_reasons",
    "coverage"
  ],
  "params": {
    "ecosystems": [
      "cargo",
      "npm"
    ]
  },
  "missing_behavior": [
    "unknown",
    "exclude"
  ],
  "confounders": [
    "unsupported_expression != missing_dependency",
    "lockfile_generation"
  ],
  "fixture_references": [
    "F022"
  ],
  "cost_class": "low",
  "privacy_class": "project_aggregate",
  "status_note": "published via `rh_cli deps` (rh-deps-metrics/1) over the parsed lockfile/manifest of a scanned project, with a per-ecosystem breakdown; counts requirement expressions outside a DECLARED supported syntax subset (semver operators/identifiers only), so protocol/alias/workspace/path forms and dist-tags are unsupported-by-declaration, not missing dependencies (F022)",
  "subject_note": "counted over every requirement expression parsed from the pinned lockfile/manifest (all packages in the graph), not only those reachable from the root",
  "measurement_class": "derived"
}
