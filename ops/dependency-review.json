{
  "review_version": "rh-dependency-review/1",
  "reviewed_at": "2026-09-20",
  "notes": "M07-05 own-dependency review. Records every external surface this implementation actually depends on, why it is acceptable in a minimal stack, and how it is isolated. Native surfaces are called out because they are the hardest to replace. There is no package-manager dependency graph here: the toolchain is pinned, the OS tools are named, and HTTPS resolution uses only Python's isolated standard library before the guarded cURL transport.",
  "dependencies": [
    {
      "id": "elisa-stage1",
      "kind": "compiler_toolchain",
      "source": "sibling Elisa-compiler checkout",
      "pinned": "TOOLCHAIN.md (stage1 snapshot rev)",
      "isolation": "build-time only; not present in produced binaries",
      "update_procedure": "bump pinned rev, rebuild, run tools/check.sh; no `latest` in automation"
    },
    {
      "id": "libc",
      "kind": "native_surface",
      "source": "operating system",
      "pinned": "no version pin (OS-provided)",
      "isolation": "only through a small declared extern set (system, mkdir, getenv, time, socket/fd calls) in rh_base and rh_http",
      "update_procedure": "externs are minimal and documented; a libc change is caught by the suite"
    },
    {
      "id": "git-cli",
      "kind": "os_tool",
      "source": "operating system",
      "pinned": "version captured into every scan's evidence (git-version.txt)",
      "isolation": "invoked only via allowlisted arguments; plumbing commands only",
      "update_procedure": "tool version is evidenced per scan; a format change fails the log parse rather than producing wrong numbers"
    },
    {
      "id": "curl",
      "kind": "os_tool",
      "source": "operating system",
      "pinned": "no version pin; flags are constants",
      "isolation": "user config disabled, proxies bypassed, --resolve pins all validated DNS answers, proto restricted to https+file, redirects disabled, byte/time caps, no auth header",
      "update_procedure": "flag set is fixed in rh_git; a change requires re-running the transport tests"
    },
    {
      "id": "python3",
      "kind": "runtime_tool",
      "source": "operating system",
      "pinned": "not pinned; Python 3 executable must be on PATH for HTTPS fetches",
      "isolation": "used only for bounded HTTPS hostname resolution via python3 -I -S -c; imports standard-library socket and threading only; resolver output is capped and each address passes rh_addr_guard before cURL --resolve",
      "update_procedure": "a resolver or interpreter change requires the M07 DNS pinning tests and full tools/check.sh gate"
    }
  ],
  "rules": [
    "runtime network access only through the guarded transport, never from metrics or policy",
    "no package manager resolves dependencies at build or run time",
    "native surfaces (libc) are the smallest declared extern set and are named here",
    "an added dependency requires an update to this review and an ADR if it changes the stack",
    "the HTTPS resolver runs only with isolated Python standard-library imports; no third-party Python package is loaded"
  ],
  "not_covered": [
    "no transitive package SBOM is produced because there is no package manager in the stack",
    "OS tool versions other than git are not evidenced per run"
  ]
}
